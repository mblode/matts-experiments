// Gesture detection
export const EDGE_ZONE_PERCENT = 0.15; // 15% from edge for peel detection
export const PEEL_THRESHOLD = 0.5; // Progress needed to complete peel
export const PEEL_DISTANCE_FACTOR = 0.7; // Normalization for directional progress

// Animation timing
export const ANIMATION_DURATION_MS = 300;

// Curl rendering
export const CURL_MIN_RADIUS = 0.05;
export const CURL_MAX_RADIUS = 0.15;

// Note dimensions
export const NOTE_SIZE = 300;

// Spring physics defaults
export const SPRING_STIFFNESS = 180;
export const SPRING_DAMPING = 12;
export const SPRING_MASS = 1;

// Momentum threshold for completion
export const MOMENTUM_THRESHOLD = 2.0;
export const MOMENTUM_PROGRESS_MIN = 0.3;
