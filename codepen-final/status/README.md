# CodePen – Status

Popover menu to set user status with animated emoji icons

## Pen Details
- **Title:** Status
- **Description:** Popover menu to set user status with animated emoji icons
- **Tags:** react, ui, animation, status, popover

## HTML (CodePen HTML panel)
Paste the contents of `index.html`:

<div id="root"></div>

## CSS (CodePen CSS panel)
Paste the contents of `index.css`.

## JS / TS (CodePen JS panel)
1. In **Pen Settings → JS → JavaScript Preprocessor**, select **TypeScript**.
2. Paste the contents of `index.ts` into the JS panel.
3. Do **not** add packages in CodePen. The imports in `index.ts` use pinned CDN URLs to keep React on a single version and avoid hook errors.

## HTML <head> (Pen Settings → HTML → Stuff for <head>)
Add Tailwind browser CDN:

<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

## CSS Settings
- CSS Preprocessor: **None**
- CSS Base: **Neither**
- Vendor Prefixing: **Neither**

## Notes
- Requires an internet connection to load CDN + npm packages.
